sig.level <- function(
  df, # data frame with data
  col.name1, # character with col name with value 1
  sd.col1, # character with col name with standart deviation of col.name1
  col.name2, # character with col name with value 2
  sd # # character with col name with standart deviation of col.name2){
  
  }